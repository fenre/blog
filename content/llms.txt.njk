---
permalink: /llms.txt
eleventyExcludeFromCollections: true
---
# {{ metadata.title }}

> {{ metadata.description }}

## For automated systems

- Norwegian (default) post list: {{ '/' | fullSiteUrl(metadata.siteUrl) }}
- English post list: {{ '/en/' | fullSiteUrl(metadata.siteUrl) }}
- RSS (Norwegian posts): {{ '/feed.xml' | fullSiteUrl(metadata.siteUrl) }}
- RSS (English posts): {{ '/en/feed.xml' | fullSiteUrl(metadata.siteUrl) }}
- Sitemap: {{ '/sitemap.xml' | fullSiteUrl(metadata.siteUrl) }}
- This file: {{ '/llms.txt' | fullSiteUrl(metadata.siteUrl) }}

## Source

Posts were exported from the Blogger-backed site. The canonical base URL for this build is `{{ metadata.siteUrl | stripTrailingSlash }}` — default in `content/_data/metadata.base.json`; override with env `SITE_URL` (e.g. GitHub Actions for Pages). Each post is available in Norwegian and English (`/posts/...` and `/en/posts/...`).
